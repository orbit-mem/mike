import { AuthDividerUI } from "@/shared/ui/AuthDividerUI";

const meta = { title: "Shared UI / AuthDivider" };
export default meta;

export const Default = () => (
    <div className="w-full max-w-80">
        <AuthDividerUI />
    </div>
);
